#include <iostream>
#include <string>

using namespace std;

enum Status {
    Active = 1,
    Paused = 2,
    Stopped = 3
};

int main() {
    cout << "Press Enter to exit...";
    cin.get();

    return 0;
}
